node_modules/
.svelte-kit/
build/
.env
.env.*
!.env.example
nodejs/
installers/
node-lts.*
node-install.log
*.log
.DS_Store
# Local AI runtime state
.atl/

# Local AI agent instructions (never expose to public repo)
AGENTS.md

# Local design-system handoff (exposes absolute machine paths — never public)
docs/design-system-migration.md
# Local codegraph index
.codegraph/

# Supabase local CLI state (project ref, versions, linked-project) - never commit
supabase/.temp/

# Cloudflare Pages/Wrangler local state and D1 bindings
.wrangler/
